{
  "name": "email-mcp-server",
  "version": "1.0.0",
  "description": "Custom MCP server for email sending (SMTP) and receiving (IMAP) — QQ Mail compatible",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "nodemailer": "^6.9.0",
    "imapflow": "^1.0.0"
  }
}

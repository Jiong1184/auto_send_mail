{
  "name": "demos",
  "version": "1.0.0",
  "description": "A lightweight AI-powered email outreach system that runs entirely within **Claude Code** — no web server, no database server, no separate application. Built with MCP servers (backend) + a Skill (frontend).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "imapflow": "^1.4.8",
    "nodemailer": "^9.0.3",
    "sql.js": "^1.14.1"
  }
}
